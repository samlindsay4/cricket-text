# Server Configuration
PORT=3000

# Admin Password
# Change this to a secure password in production
ADMIN_PASSWORD=ashes2025

# Railway Volume Mount Path (Production)
# Set this in Railway to point to your volume mount path
# Example: /app/data-persistent
# Leave unset for local development (will use ./data)
# RAILWAY_VOLUME_MOUNT_PATH=/app/data-persistent
